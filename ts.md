## Type by inference
```let helloWorld = "Hello world!" (type of variable is automatically inferred to be of string)```

## Defining types
```interface User {
    "name": string;
    id: number;
}

const user: User = {
    "name": "vikas",
    "id": 1
}
```

